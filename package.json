{
  "name": "restaurant-search-api",
  "version": "1.0.0",
  "description": "Backend service for searching restaurants based on dishes with price range filtering",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "node scripts/migrate.db.js",
    "seed": "node scripts/seed.db.js",
    "setup": "npm run migrate && npm run seed",
    "lint": "eslint src --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "restaurant",
    "search",
    "api",
    "nodejs",
    "express",
    "mysql",
    "es6",
    "import",
    "middleware"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.22.1",
    "morgan": "^1.10.1",
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "eslint": "^8.52.0",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
